{
  "version": 2,
  "buildCommand": "npm run db:generate && npm --workspace frontend run build && npm --workspace backend run build",
  "outputDirectory": "frontend/.next",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...path]"
    }
  ]
}
